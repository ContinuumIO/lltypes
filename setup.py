from distutils.core import setup

setup(
    name = "lltypes",
    license = "BSD",
    description = "Generic types",
    packages = ['lltypes',
                'lltypes.tests',],
    version = "dev",
)
